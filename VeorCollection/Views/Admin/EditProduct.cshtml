@model VeorCollection.Models.Product

@{
    ViewData["Title"] = "Ürünü Düzenle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <h2>Ürünü Düzenle: @Model.Name</h2>
    <hr />
    <div class="row">
        <div class="col-md-8">
            <form asp-action="EditProduct" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="ImageUrl" />

                <div class="row">
                    <div class="col-md-6 form-group mb-3">
                        <label class="control-label fw-bold">Ürün Adı</label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 form-group mb-3">
                        <label class="control-label fw-bold">Fiyat</label>
                        <input asp-for="Price" type="number" step="0.01" class="form-control" />
                    </div>
                </div>

                <div class="form-group mb-3">
                    <label class="control-label fw-bold">Yeni Resim Yükle</label>
                    <input type="file" name="imageFile" class="form-control" accept="image/*" />
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <div class="mt-2 p-2 border rounded bg-light">
                            <small class="text-muted">Mevcut:</small>
                            <img src="@Model.ImageUrl" style="height: 80px;" />
                        </div>
                    }
                </div>

                <div class="form-group mb-3">
                    <label class="control-label fw-bold">Kategori</label>
                    <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.Categories"></select>
                </div>

                <div class="row">
                    <div class="col-md-6 form-group mb-3">
                        <label asp-for="Cinsiyet" class="control-label fw-bold">Cinsiyet</label>
                        <select asp-for="Cinsiyet" class="form-control">
                            <option value="">Seçiniz...</option>
                            <option value="Erkek">Erkek</option>
                            <option value="Kadın">Kadın</option>
                            <option value="Unisex">Unisex</option>
                        </select>
                        <span asp-validation-for="Cinsiyet" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 form-group mb-3">
                        <label asp-for="KokuTipi" class="control-label fw-bold">Koku Tipi</label>
                        <select asp-for="KokuTipi" class="form-control">
                            <option value="">Seçiniz...</option>
                            <option value="Çiçeksi">Çiçeksi</option>
                            <option value="Odunsu">Odunsu</option>
                            <option value="Meyveli">Meyveli</option>
                            <option value="Baharatlı">Baharatlı</option>
                            <option value="Ferah">Ferah</option>
                            <option value="Şekerli">Şekerli</option>
                        </select>
                        <span asp-validation-for="KokuTipi" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group mb-3">
                    <label class="control-label fw-bold">Stok Kodu (SKU)</label>
                    <input asp-for="SKU" class="form-control" />
                </div>

                <div class="form-group mb-3">
                    <label class="control-label fw-bold">Kısa Açıklama</label>
                    <textarea asp-for="ShortDescription" class="form-control" rows="3"></textarea>
                </div>

                <div class="form-group mb-3">
                    <label class="control-label fw-bold">Uzun Ürün Detayları</label>
                    <textarea asp-for="FullDescription" class="form-control" rows="6"></textarea>
                </div>

                <div class="form-check mb-4">
                    <input class="form-check-input" type="checkbox" asp-for="IsInStock" />
                    <label class="form-check-label">Stokta Var</label>
                </div>

                <button type="submit" class="btn btn-warning">Güncelle</button>
                <a href="/Admin/Products" class="btn btn-secondary">İptal</a>
            </form>
        </div>
    </div>
</div>